# 组件维护约定

## 仓库关系

本仓库维护组件目录、版本组合、接入说明和 CI；驱动源码在对应的独立仓库维护。只修改总仓库里的子模块工作区而未推送子模块提交，会导致其他人无法克隆。先将组件提交同步到 GitHub 与 Gitee，再同步总仓库，保证两个平台都能获取引用。

更新现有组件时，在总仓库根目录执行，例如：

```sh
git -C lib/stm_flash fetch origin
git -C lib/stm_flash switch --detach <已经推送的commit或tag>
git add lib/stm_flash
git commit -m "更新 stm_flash 版本引用"
```

随后运行 CI，检查说明与依赖兼容性，再合入总仓库。开发组件时在独立仓库或子模块内显式创建分支；`submodule update` 通常会使子模块处于 detached HEAD。

## 新增组件

1. 创建公开独立仓库，提供源码、头文件、CMake 接入、README 和明确的许可证。
2. 在 GitHub 与 Gitee 的同一账号下分别创建同名公开组件仓库。在 `lib/` 添加 submodule，使用 `../stm_<name>.git` 相对地址，固定一个已推送到两个平台的提交。
3. 更新总 README 的组件表，明确 MCU/HAL/器件支持范围、依赖和已完成的验证。
4. 按组件功能增加代表性编译检查和有价值的软件测试；硬件测试须单独说明条件和结果。

```sh
git submodule add ../stm_<name>.git lib/stm_<name>
```

默认目录结构可参考 Flash/SDRAM：根目录放 `.c`、`.h`、`CMakeLists.txt`、`README.md`、`LICENSE`；`example/main.c` 提供最小参考；需要时提供 `tests/`。不要求空目录、CHANGELOG 或额外 docs。

## 接口与错误码

- 组件名使用 `stm_<功能>`；新设备驱动函数使用 `<功能>_<动作>`，例如 `flash_create`、`sdram_read16`。类型、枚举和宏使用组件专属前缀，避免全局命名冲突。
- 有实例生命周期的设备优先使用不透明 `<功能>_handle_t`、`<功能>_config_t`、`create(&config, &handle)` 和 `delete(&handle)`。无实例的工具组件不强制创建句柄；现有 `stm_log` 保留自己的 API。
- 操作返回值统一使用 `stm_common` 的 `stm_err_t`；成功为 `STM_OK`，检查 `err != STM_OK`。公共错误优先复用，专用错误区间在 `stm_common` 登记，不能各自复制公共码或直接暴露 HAL 状态为统一错误码。
- 参数含义、单位、对齐、零长度、缓冲区生命周期、失败后状态和部分完成行为须明确。HAL 句柄由板级管理，组件不能擅自释放或重新配置共享资源。
- 创建失败回收对象；删除成功清空传入的句柄。明确动态分配策略、线程/中断限制和并发责任，不暗示默认线程安全。
- 新驱动不强制依赖日志、RTT、RTOS；可选功能须说明资源与行为代价。

## 注释与示例

- `.c` / `.h` 文件头用简洁的 `@file` 和 `@brief`。
- 公共头文件用 Doxygen 描述契约：用途、参数、返回值和必要限制；实现内部用简短中文 `//` 说明原因与硬件约束。
- 结构体字段优先使用行内注释；避免复述代码、重复作者/日期/修改流水账。
- 最小示例入口为 `main()`，板级初始化集中于 `board_init()`。示例本身不提供给其他地方调用，不增加示例头文件或无关日志依赖。
- 示例若擦写或覆盖存储，标出范围与条件；板级时钟和 GPIO 仅作为明确型号的参考。
- 已有第三方代码必须保留其要求的版权与授权信息，不能为统一格式而删除。

## 验证与发布

编译须检查警告；软件测试关注边界、故障路径和资源生命周期。修改子模块引用或 CI 后运行 [ci/README.md](ci/README.md) 中的检查。新增系列和器件须提供对应验证，不能仅因编译成功就宣称硬件支持。

单个组件按自身节奏发布 tag；总仓库发布时固定通过检查的组合，并在 Release 中说明实际变化和验证范围。测试日志、固件、探针信息及开发过程文档保留本地，不放入公开分发目录。
